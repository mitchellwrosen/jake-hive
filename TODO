Things to do:

Need to have:
- finish implementing updateGame
- serialize game state to JSON
- parse moves from JSON

Nice to have:
- validate move
- undo functionality
- given a piece, return valid locations to move it
- define how a turn happens (place/move, pass turn)
